const Broadcast = () => {
  return (
    <div>
      <h2 style={{ fontSize: "24px", fontWeight: "bold" }}>Send Broadcast Messages</h2>
      <p>Here you can compose and send broadcast messages to users.</p>
      {/* Add more functionality as needed */}
    </div>
  );
};

export default Broadcast;
